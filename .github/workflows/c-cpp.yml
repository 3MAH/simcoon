name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  conda-setup:

    runs-on: ubuntu-latest

    steps:
    - uses: conda-incubator/setup-miniconda@v2
    - uses: actions/checkout@v2
      with:
        miniconda-version: "latest"
        activate-environment: foo
    
    - name: Dependencies
      shell: bash -l {0}
      run: | 
        conda install -c conda-forge cgal armadillo boost
        cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=Release
  
  # build:
  #   runs-on: ubuntu-latest
    
  #   steps:
  #   - uses: actions/checkout@v2
    
  #   - name: Dependencies
  #     run: | 
  #       $CONDA/bin/conda install -c conda-forge cgal armadillo boost
        
  #   - name: CMake
  #     run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=Release
      
   #  - name: Build
   #    run: cmake --build ${{github.workspace}}/build
    # - name: Tests
    #   run: make test
