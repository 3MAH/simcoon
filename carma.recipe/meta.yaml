package:
  name: carma
  version: 0.8.0

source:
  git_url: https://github.com/RUrlus/carma
  git_tag: v0.8.0

build:
  number: 0

requirements:
  build:
    - git # [win]
    - cmake
    - {{ compiler('cxx') }}
    - ninja  # [win]
  host:
    - armadillo
    - pybind11
  run:
    - armadillo
    - pybind11
