package:
  name: carma
  version: 0.6.7

source:
  git_url: https://github.com/RUrlus/carma
  git_tag: v0.6.7

build:
  number: 0

requirements:
  build:
    - git # [win]
    - cmake
    - python
    - numpy
    - {{ compiler('cxx') }}
    - ninja  # [win]
  host:
    - python
    - pip
    - armadillo
    - numpy
    - pybind11
  run:
    - libblas
    - liblapackon
    - python
    - armadillo
    - numpy
    - pybind11
